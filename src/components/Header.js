import React from 'react'

const Header = ({ title }) => {
    return (
        <div className="panel">
            <header className="header">
                <h2>{title}</h2>
            </header>

        </div>
    )
}

export default Header
